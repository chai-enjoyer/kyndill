// Placeholder hook. Real shop catalogue + purchase wiring lands with the shop screen.
export function useShop(): { items: unknown[]; coins: number; isLoading: boolean } {
  return { items: [], coins: 0, isLoading: false };
}
