// Placeholder hook. Real list/create/update/complete wiring lands when the
// habits screens are built.
export function useHabits(): { habits: unknown[]; isLoading: boolean } {
  return { habits: [], isLoading: false };
}
