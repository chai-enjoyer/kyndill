// Placeholder hook. Real friends/requests/gifts wiring lands with the social screens.
export function useSocial(): { friends: unknown[]; requests: unknown[]; isLoading: boolean } {
  return { friends: [], requests: [], isLoading: false };
}
